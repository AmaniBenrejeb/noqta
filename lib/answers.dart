import 'package:flutter/material.dart';

class MyAnswers extends StatelessWidget {
  const MyAnswers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Column(
      children: [Text("MY ANSWERS")],
    ));
  }
}
